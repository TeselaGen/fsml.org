export const enum ManifestTypes {
  PROTOCOL = "protocol",
  DATA = "data",
}

export const enum FormatTypes {
  JSON = "json",
  YAML = "yaml",
  TOML = "toml",
}

export const enum PackTypes {
  ZIP = "zip",
  TAR = "tar",
  TGZ = "tgz",
}
