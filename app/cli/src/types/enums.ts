export enum ManifestTypes {
  PROTOCOL = "protocol",
  DATA = "data",
}

export enum FormatTypes {
  JSON = "json",
  YAML = "yaml",
  TOML = "toml",
}

export enum PackTypes {
  ZIP = "zip",
  TAR = "tar",
  TGZ = "tgz",
}
