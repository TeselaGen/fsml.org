export enum ManifestTypes {
  PROTOCOL = "protocol",
  DATA = "data",
}

export enum FormatTypes {
  JSON = "json",
  YAML = "yaml",
  TOML = "toml",
}

export enum PackTypes {
  ZIP = "zip",
  TAR = "tar",
  TGZ = "tgz",
}

export enum DataFileFormats {
  CSV = "csv",
  XSLX = "xslx",
  JSON = "json",
  YAML = "yaml",
  TOML = "toml",
}
